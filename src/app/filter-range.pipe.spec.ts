import { FilterRangePipe } from './filter-range.pipe';

describe('FilterRangePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRangePipe();
    expect(pipe).toBeTruthy();
  });
});
